# ============================================================================
# PostAssist V2 — FAQAT TEST uchun bog'liqliklar
#
# Production requirements.txt ga QO'SHILMAYDI (Docker image'ga kirmaydi).
# Test runner'dagi ba'zi suite'lar bu paketlar bo'lmasa o'zini O'TKAZIB
# YUBORADI (skip) — ya'ni ular o'rnatilmasa "yashil" natija to'liq qamrovni
# anglatmaydi. Shu sababli CI va lokal yugurtirishlarda o'rnatish tavsiya
# etiladi:
#
#     python -m pip install -r telegram_bot/requirements.txt \
#                           -r tests/requirements-test.txt
#
# Nima uchun kerak:
#   pytest    — telegram_bot/tests/p0_concurrency_test.py (real PostgreSQL
#               row-lock/unique constraint testlari) shu runner orqali yuradi;
#               bo'lmasa suite "pytest topilmadi" deb o'tkazib yuboriladi.
#   pgserver  — lokal vaqtinchalik PostgreSQL ko'taradi. Bo'lmasa live DB
#               suite'lari (load / stress / integrity / P0 concurrency va
#               tests/concurrency_load_test.py DB qismi) SKIP bo'ladi va
#               concurrency test "NOT TESTED — resource limit" deb qayd etadi.
#               DIQQAT (P2 tuzatildi): ilgari bu yerda mavjud BO'LMAGAN
#               "pgserver>=0.2.0" yozilgan edi — PyPI'dagi eng yuqori reliz
#               0.1.4. Natijada `pip install -r tests/requirements-test.txt`
#               XATO bilan to'xtab, live-DB suite'lari JIM skip bo'lardi
#               (soxta yashil). Endi real mavjud versiya pin qilingan.
#   pyflakes  — PHASE 14 (56-band) dead-code/unused-import tozaligini
#               tekshirish uchun (qo'lda: python -m pyflakes telegram_bot tests).
# ============================================================================

pytest>=8.0
pgserver>=0.1.4
pyflakes>=3.2.0
